// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
  // url      = env("DATABASE_URL")
   
}

model User {
  id       Int     @id @default(autoincrement())
  email    String  @unique
  password String
  age      Int?
  name String
}


model Food {
  id Int @id @default(autoincrement())
  name String 
  price String 
  category FoodCategory @relation( fields: [FoodCategoryId], references: [id])
  FoodCategoryId Int

  createAt DateTime @default(now())
  updatedAt DateTime @updatedAt()
}

model FoodCategory{
  id Int @id @default(autoincrement())
  name String
  foods Food[]

  createAt DateTime @default(now())
  updatedAt DateTime @updatedAt()
}

// model FoodOrder {
//   id Int @id @default(autoincrement())
//   totalPrice String
//   status String

//   createAt DateTime @default(now())
//   updatedAt DateTime @updatedAt()
// }

// model FoodOrderItem {
//   id Int @id @default(autoincrement())
//   quantity Int
//   food Food @relation(fields: [],references: [id])
//   foodId Int 
//   order FoodOrder 

// }
